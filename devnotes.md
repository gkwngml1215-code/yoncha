# 연차 관리 보드 — 개발자 노트

## 개요
직원 5명(가변) 규모 팀을 위한 연차/반차 관리 도구. 단일 HTML 파일로 작성된 Claude Artifact이며, `db` 런타임 capability를 통해 서버에 데이터를 저장해 여러 사용자가 동시에 같은 데이터를 보고 편집할 수 있다.

- **배포 URL**: https://claude.ai/code/artifact/ba3118a7-4c17-433d-a276-5e8637ee674d
- **소스 파일**: `leave-tracker.html` (스크래치패드에 보관, 필요 시 재요청하여 로컬 저장 가능)
- **런타임 contract**: 0.2.41

## 기술 스택
- 순수 HTML/CSS/JS (프레임워크 없음), IIFE 하나로 전체 로직 구성
- 폰트: Fraunces(제목), Public Sans(본문), IBM Plex Mono(숫자) — Google Fonts CDN
- 데이터 저장: `window.claude.use("db")` — Firestore 유사 문서 스토어
- `db`를 사용할 수 없는 뷰(예: 저장된 파일로 직접 열람)에서는 `localStorage`로 폴백하여 최소 기능은 유지

## 데이터 모델
Firestore 스타일 컬렉션 두 개.

### `employees/{empId}`
```json
{ "name": "직원 1", "totalDays": 15 }
```
- `empId`는 `db.collection("employees").add()`가 자동 생성 (신규 추가 시) 또는 초기 시드 시 `emp1`~`emp5`로 수동 지정
- `totalDays`: 연간 부여 연차 일수, 카드에서 인라인으로 수정 가능

### `leaves/{leaveId}`
```json
{
  "employeeId": "emp1",
  "type": "연차",
  "start": "2026-09-10",
  "end": "2026-09-12",
  "days": 3,
  "memo": "가족 여행",
  "createdAt": "2026-09-06T09:00:00.000Z"
}
```
- `type`: `"연차"` | `"반차"` 두 가지만 허용 (병가/경조사/기타 종류는 요구사항 변경으로 제거됨)
- `days`: 연차는 시작~종료일 사이 평일(월~금) 수를 자동 계산, 반차는 항상 `0.5`이며 종료일=시작일로 고정
- 모든 기록은 항상 연차에서 차감됨 (초기 버전에 있던 "차감 여부" 체크박스는 종류가 2개로 줄면서 제거)

## 핵심 비즈니스 로직
- **평일 계산**: `countWeekdays(start, end)` — 토/일 제외 단순 카운트. 공휴일은 반영하지 않음 (필요 시 공휴일 테이블 추가 검토)
- **잔여 일수**: `totalDays - Σdays` (해당 직원의 모든 leaves 합산)
- **색상 임계값**: 잔여 5일 이상 초록, 1~4일 노랑, 0일 이하 빨강
- **과거 기록 잠금**: `leave.end < 오늘`이면 `isLocked()`가 true → 삭제 버튼 대신 "완료됨" 라벨 표시, 수정 불가. 신청 시점과 무관하게 **휴가 종료일이 지나면 즉시 잠김**
- **직원 삭제 시 연쇄 삭제**: 직원을 삭제하면 해당 `employeeId`를 가진 leaves 문서도 함께 삭제 (고아 데이터 방지)

## UI 상호작용 메모
- 이름/총 연차 일수는 클릭 후 값 변경 시 `change` 이벤트로 즉시 저장 (별도 저장 버튼 없음)
- 삭제(직원/휴가 기록 공통)는 2단계 확인 패턴 사용 — 네이티브 `confirm()` 대신 버튼 텍스트가 "삭제" → "정말 삭제?"로 바뀌는 방식 (샌드박스 iframe에서 네이티브 confirm이 차단될 수 있어 회피)
- 상단 pill(`#syncDot`/`#syncLabel`)로 db 연결 상태를 실시간 표시: "실시간 동기화됨" vs "이 브라우저에만 저장됨"

## 알려진 제한 사항 / 추후 개선 여지
- 공휴일 미반영 (평일 = 근무일로 단순 가정)
- 연차 기록에 대한 "수정" 기능은 없음 (등록 후 미래 날짜 건은 삭제 후 재등록만 가능)
- 직원별 입사일 기반 자동 연차 부여(1년 미만 월차 등) 로직 없음 — `totalDays`는 관리자가 수동 입력
- db 미연결 시 로컬 저장(`localStorage`)은 브라우저/기기별로 분리되어 다른 사람과 공유되지 않음

## 변경 이력
1. 최초 버전: 종류 6종(연차/오전반차/오후반차/병가/경조사/기타), 종류별 차감 여부 체크박스, 직원 5명 고정
2. 요청에 따라 축소: 종류를 연차/반차 2종으로 축소, 차감 체크박스 제거(항상 차감), 종료일이 지난 기록 잠금 추가, 직원 추가/삭제 기능 추가(고정 5명 → 가변)
